apiVersion: apps/v1
kind: Deployment
metadata:
  name: reniec-back
  labels:
    app: reniec-back1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reniec-back1
  template:
    metadata:
      labels:
        app: reniec-back1
    spec:
      containers:
        - name: reniec-back1
          image: josefirstacr.azurecr.io/test
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      imagePullSecrets:
        - name: regcred
---
kind: Service
apiVersion: v1
metadata:
   name: reniec-balancer
spec:
  selector:
    app: reniec-balancer
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer